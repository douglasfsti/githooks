# https://travis-ci.org/douglasfsti/githooks

language: python

python:
    - "2.7"
    - "3.5"

# command to install dependencies
install:
    - pip install --user -r requirements.txt

# command to run tests
script: nosetests

branches:
    only:
        - master

